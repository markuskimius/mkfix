# Auto-acknowledge
#
# Shows:   the smallest useful macro — match an order, wait, accept it
# Needs:   any session that receives orders
# Watch:   Received Orders: Pending goes from New to blank, Status to New
# Outcome: every order is accepted 250 ms after it arrives

macro auto-ack

on order
    after 250ms
    accept
